export class Hero {
    public name: string;
    public color: string;
    public head: string;
    public body: string;

    constructor(name: string, color: string, head: string, body: string) {
        this.name = name;
        this.color = color;
        this.head = head;
        this.body = body;
    }
}